export default function HomePage() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-lg flex-col justify-center px-6 py-16">
      <p className="text-sm tracking-[0.2em] uppercase text-[var(--ink-muted)]">
        tint
      </p>
      <h1 className="mt-4 text-4xl leading-tight text-[var(--ink)]">
        Map how you connect
      </h1>
      <p className="mt-4 text-lg text-[var(--ink-muted)]">
        Two partners, one device, ten intimacy dimensions. Scaffolding in
        progress.
      </p>
    </main>
  );
}
